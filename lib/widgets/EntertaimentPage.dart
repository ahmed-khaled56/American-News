import 'package:flutter/material.dart';

class entertaimentPage extends StatelessWidget {
  const entertaimentPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
